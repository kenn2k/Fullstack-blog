export const STRATEGY = {
  JWT: 'jwt',
  JWT_REFRESH: 'jwt-refresh',
  LOCAL: 'local',
} as const;

export const COOKIE = {
  ACCESS_TOKEN: 'access_token',
  REFRESH_TOKEN: 'refresh_token',
} as const;
